# 创建项目
vue create hk-guide

# 进入项目目录
cd hk-guide

# 安装必要的依赖
npm install element-plus @element-plus/icons-vue vue-router axios pinia sass

# 安装开发依赖
npm install -D @vitejs/plugin-vue eslint eslint-plugin-vue sass

# 启动开发服务器
npm run dev

mkdir -p src/{components,views,router,data,styles}

# 检查 Node.js 版本
node -v

# 检查 npm 版本
npm -v

# 在终端中找到 npm 的具体位置
which npm

# 清除 npm 缓存
npm cache clean --force

# 重新设置镜像并安装
npm config set registry https://registry.npmmirror.com
npm install -g npm

# 验证是否设置成功
npm config get registry

# 安装 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com

# 然后使用 cnpm 安装依赖
cnpm install

# 停止当前服务（如果正在运行）
# 按 Ctrl+C

# 删除 node_modules
rm -rf node_modules

# 删除 package-lock.json
rm package-lock.json

# 重新安装依赖
npm install

# 启动开发服务器
npm run dev

# 检查项目结构
ls -R src/

# 如果目录不存在，创建必要的目录
mkdir -p src/{components,views,router,data}

# 安装主要依赖
npm install mitt pinia @vueuse/core sass

# 如果之前没有安装 Element Plus 的话，也需要安装
npm install element-plus @element-plus/icons-vue

# 创建图片目录
mkdir -p src/img

# 安装 date-fns
npm install date-fns

# 安装 gh-pages 包
npm install --save-dev gh-pages

# 在项目根目录下初始化 git
git init

# 添加所有文件到暂存区
git add .

# 创建第一次提交
git commit -m "Initial commit"

# 添加远程仓库（将 YOUR_USERNAME 和 REPO_NAME 替换为你的 GitHub 用户名和仓库名）
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 设置主分支名称为 main
git branch -M main

# 推送到远程仓库
git push -u origin main

# 添加 .gitignore 后
git add .gitignore
git commit -m "Add .gitignore"
git push

# 检查远程仓库设置
git remote -v

# 检查当前状态
git status

# 当你修改了代码后
git add .
git commit -m "你的提交信息"
git push

# 如果远程有更新，先拉取
git pull

# 添加所有更改
git add .

# 提交更改
git commit -m "Setup GitHub Pages deployment"

# 推送到 GitHub
git push origin main

# 如果部署失败，可以尝试清理缓存
npm clean cache --force

# 重新安装依赖
rm -rf node_modules
npm install

# 本地测试构建
npm run build
npm run preview